---
project: application

tasks:
  build:
    cmd: |-
      echo "This is a fake build task"
  deploy:
    cmd: |-
      sleep 60
      echo "This is a fake deployment task"
  mark_ci_ready:
    shell: bash
    cmd: ./mark_ci_ready.sh
